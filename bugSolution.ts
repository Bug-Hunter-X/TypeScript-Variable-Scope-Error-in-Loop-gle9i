function printNumbers(n: number): void {
  for (let i = 1; i <= n; i++) {
    console.log(i);
  }
}

printNumbers(5);

function printNumbers2(n: number): void {
  let i:number;
  for (i = 1; i <= n; i++) {
    console.log(i);
  }
}

printNumbers2(5); 